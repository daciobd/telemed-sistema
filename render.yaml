services:
  - type: web
    name: telemed-sistema
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node render-start.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          type: web
          name: telemed-sistema
          property: port