services:
  - type: web
    name: telemed-sistema
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - telemed-sistema.onrender.com