<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teste Transtorno Bipolar - MDQ</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .question-card {
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .question-card.answered {
            border-color: #10b981;
            background-color: #f0fdf4;
        }
        
        .checkbox-option {
            transition: all 0.2s ease;
        }
        
        .checkbox-option:hover {
            background-color: #f3f4f6;
        }
        
        .progress-bar {
            transition: width 0.3s ease;
        }
        
        .result-card {
            animation: slideIn 0.5s ease-out;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .hidden {
            display: none;
        }
        
        .gradient-bg {
            background: linear-gradient(45deg, #8b5cf6, #7c3aed, #6366f1);
            background-size: 400% 400%;
            animation: gradientShift 3s ease infinite;
        }
        
        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }
        
        .step-indicator {
            transition: all 0.3s ease;
        }
        
        .step-indicator.active {
            background-color: #8b5cf6;
            color: white;
        }
    </style>
</head>
<body class="gradient-bg">
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-4xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-8">
                <div class="inline-block bg-white/20 text-white text-sm font-bold px-4 py-2 rounded-full mb-4">
                    🔄 AVALIAÇÃO TRANSTORNO BIPOLAR
                </div>
                <h1 class="text-4xl font-bold text-white mb-4">
                    MDQ - Questionário de Transtorno do Humor
                </h1>
                <p class="text-xl text-white/90 max-w-3xl mx-auto">
                    Instrumento de rastreamento para episódios maníacos ou hipomaníacos
                </p>
                <p class="text-white/70 mt-2">
                    Identifique se você já experimentou períodos de humor elevado característicos do transtorno bipolar
                </p>
            </div>

            <!-- Step Indicators -->
            <div class="flex justify-center mb-8">
                <div class="flex space-x-2">
                    <div id="step1" class="step-indicator w-8 h-8 rounded-full bg-white/20 flex items-center justify-center text-white text-sm font-bold">1</div>
                    <div id="step2" class="step-indicator w-8 h-8 rounded-full bg-white/20 flex items-center justify-center text-white text-sm font-bold">2</div>
                    <div id="step3" class="step-indicator w-8 h-8 rounded-full bg-white/20 flex items-center justify-center text-white text-sm font-bold">3</div>
                </div>
            </div>

            <!-- Progress Bar -->
            <div class="bg-white/20 rounded-full h-3 mb-8">
                <div id="progressBar" class="progress-bar bg-white h-3 rounded-full" style="width: 0%"></div>
            </div>
            <div class="text-center text-white mb-8">
                <span id="progressText">Etapa 1 de 3</span>
            </div>

            <!-- Test Form -->
            <div id="testForm">
                <!-- Content will be generated by JavaScript -->
            </div>

            <!-- Navigation Buttons -->
            <div id="navigationButtons" class="flex justify-between mt-8">
                <button id="prevBtn" class="bg-white/20 hover:bg-white/30 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    ← Anterior
                </button>
                <button id="nextBtn" class="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    Próxima →
                </button>
            </div>

            <!-- Results Section (Hidden initially) -->
            <div id="resultsSection" class="hidden">
                <div class="result-card bg-white rounded-2xl p-8 mt-8">
                    <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
                        📊 Resultados - MDQ
                    </h2>
                    
                    <div id="resultContent" class="space-y-6">
                        <!-- Results will be populated by JavaScript -->
                    </div>
                    
                    <div class="mt-8 p-6 bg-purple-50 border-l-4 border-purple-400 rounded-r-lg">
                        <h3 class="text-lg font-semibold text-purple-800 mb-2">⚠️ Importante</h3>
                        <p class="text-purple-700">
                            Este teste é uma ferramenta de triagem e não substitui a avaliação de um profissional de saúde mental. 
                            O transtorno bipolar é uma condição complexa que requer diagnóstico especializado.
                        </p>
                    </div>
                    
                    <div class="text-center mt-8 space-x-4">
                        <button onclick="resetTest()" class="bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-lg transition-colors">
                            🔄 Fazer Teste Novamente
                        </button>
                        <button onclick="goToCenter()" class="bg-indigo-600 hover:bg-indigo-700 text-white px-8 py-3 rounded-lg transition-colors">
                            🧠 Outros Testes
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // MDQ Questions - Part 1 (13 symptoms)
        const part1Questions = [
            { id: 1, text: "Você se sentiu tão bem ou energizado que outras pessoas pensaram que você não estava sendo você mesmo, ou estava tão energizado que se meteu em problemas?" },
            { id: 2, text: "Você ficou tão irritado que gritou com pessoas ou iniciou brigas ou discussões?" },
            { id: 3, text: "Você se sentiu muito mais autoconfiante do que o usual?" },
            { id: 4, text: "Você dormiu muito menos do que o usual e achou que realmente não precisava dormir?" },
            { id: 5, text: "Você ficou muito mais falante ou falou muito mais rápido do que o usual?" },
            { id: 6, text: "Os pensamentos corriam pela sua cabeça ou você não conseguia diminuir o ritmo de sua mente?" },
            { id: 7, text: "Você se distraía tão facilmente que a menor interrupção o tirava do que estava fazendo ou pensando?" },
            { id: 8, text: "Você tinha muito mais energia do que o usual?" },
            { id: 9, text: "Você estava muito mais ativo ou fez muito mais coisas do que o usual?" },
            { id: 10, text: "Você estava muito mais sociável ou extrovertido do que o usual, por exemplo, telefonou para amigos no meio da noite?" },
            { id: 11, text: "Você estava muito mais interessado em sexo do que o usual?" },
            { id: 12, text: "Você fez coisas que eram incomuns para você ou que outras pessoas poderiam ter achado excessivas, tolas ou arriscadas?" },
            { id: 13, text: "Gastar dinheiro colocou você ou sua família em problemas?" }
        ];

        let currentStep = 1;
        let part1Answers = {};
        let part2Answer = null;
        let part3Answer = null;

        // Initialize test
        function initTest() {
            renderCurrentStep();
            updateProgress();
            updateNavigation();
            updateStepIndicators();
        }

        // Render current step
        function renderCurrentStep() {
            const testForm = document.getElementById('testForm');
            
            if (currentStep === 1) {
                renderPart1();
            } else if (currentStep === 2) {
                renderPart2();
            } else if (currentStep === 3) {
                renderPart3();
            }
        }

        // Render Part 1 - Symptom checklist
        function renderPart1() {
            const testForm = document.getElementById('testForm');
            
            testForm.innerHTML = `
                <div class="question-card bg-white rounded-2xl p-8">
                    <div class="mb-6">
                        <h3 class="text-2xl font-semibold text-gray-800 mb-4">
                            Parte 1: Sintomas de Mania/Hipomania
                        </h3>
                        <p class="text-gray-600 mb-4">
                            Alguma vez na sua vida você teve um período em que você se sentiu diferente do usual quando:
                        </p>
                        <p class="text-sm text-purple-700 bg-purple-50 p-3 rounded-lg">
                            💡 Marque "Sim" apenas se os sintomas ocorreram durante o mesmo período de tempo
                        </p>
                    </div>
                    
                    <div class="space-y-3">
                        ${part1Questions.map(question => `
                            <div class="checkbox-option p-4 rounded-lg border border-gray-200 hover:bg-gray-50">
                                <label class="flex items-start cursor-pointer">
                                    <input type="checkbox" id="q${question.id}" 
                                           class="w-5 h-5 text-purple-600 border-gray-300 rounded focus:ring-purple-500 mt-1 flex-shrink-0"
                                           ${part1Answers[question.id] ? 'checked' : ''}
                                           onchange="handlePart1Answer(${question.id}, this.checked)">
                                    <span class="ml-3 text-gray-700 leading-relaxed">
                                        ${question.id}. ${question.text}
                                    </span>
                                </label>
                            </div>
                        `).join('')}
                    </div>
                    
                    <div class="mt-6 p-4 bg-blue-50 rounded-lg">
                        <p class="text-sm text-blue-700">
                            <strong>Selecionados:</strong> <span id="selectedCount">${Object.keys(part1Answers).length}</span>/13 sintomas
                        </p>
                    </div>
                </div>
            `;
        }

        // Render Part 2 - Impact question
        function renderPart2() {
            const testForm = document.getElementById('testForm');
            const selectedSymptoms = Object.keys(part1Answers).length;
            
            testForm.innerHTML = `
                <div class="question-card bg-white rounded-2xl p-8">
                    <div class="mb-6">
                        <h3 class="text-2xl font-semibold text-gray-800 mb-4">
                            Parte 2: Impacto dos Sintomas
                        </h3>
                        <p class="text-gray-600 mb-4">
                            Você marcou <strong>${selectedSymptoms} sintomas</strong> na primeira parte.
                        </p>
                        <p class="text-gray-800 text-lg font-medium">
                            Se você marcou SIM para mais de um item acima, vários desses aconteceram durante o mesmo período de tempo?
                        </p>
                    </div>
                    
                    <div class="space-y-3">
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-purple-50">
                            <input type="radio" name="part2" value="yes" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part2Answer === 'yes' ? 'checked' : ''}
                                   onchange="handlePart2Answer('yes')">
                            <span class="ml-3 text-gray-700 font-medium">✅ Sim</span>
                        </label>
                        
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-purple-50">
                            <input type="radio" name="part2" value="no" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part2Answer === 'no' ? 'checked' : ''}
                                   onchange="handlePart2Answer('no')">
                            <span class="ml-3 text-gray-700 font-medium">❌ Não</span>
                        </label>
                    </div>
                </div>
            `;
        }

        // Render Part 3 - Functional impairment
        function renderPart3() {
            const testForm = document.getElementById('testForm');
            
            testForm.innerHTML = `
                <div class="question-card bg-white rounded-2xl p-8">
                    <div class="mb-6">
                        <h3 class="text-2xl font-semibold text-gray-800 mb-4">
                            Parte 3: Prejuízo Funcional
                        </h3>
                        <p class="text-gray-800 text-lg font-medium">
                            Quanto esses sintomas causaram problemas para você - como no trabalho, na escola, com a família, ou com outras pessoas?
                        </p>
                    </div>
                    
                    <div class="space-y-3">
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-red-50">
                            <input type="radio" name="part3" value="no-problem" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part3Answer === 'no-problem' ? 'checked' : ''}
                                   onchange="handlePart3Answer('no-problem')">
                            <span class="ml-3 text-gray-700 font-medium">🟢 Sem problemas</span>
                        </label>
                        
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-yellow-50">
                            <input type="radio" name="part3" value="minor" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part3Answer === 'minor' ? 'checked' : ''}
                                   onchange="handlePart3Answer('minor')">
                            <span class="ml-3 text-gray-700 font-medium">🟡 Problemas menores</span>
                        </label>
                        
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-orange-50">
                            <input type="radio" name="part3" value="moderate" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part3Answer === 'moderate' ? 'checked' : ''}
                                   onchange="handlePart3Answer('moderate')">
                            <span class="ml-3 text-gray-700 font-medium">🟠 Problemas moderados</span>
                        </label>
                        
                        <label class="checkbox-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-red-50">
                            <input type="radio" name="part3" value="serious" 
                                   class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                   ${part3Answer === 'serious' ? 'checked' : ''}
                                   onchange="handlePart3Answer('serious')">
                            <span class="ml-3 text-gray-700 font-medium">🔴 Problemas sérios</span>
                        </label>
                    </div>
                </div>
            `;
        }

        // Handle Part 1 answers
        function handlePart1Answer(questionId, checked) {
            if (checked) {
                part1Answers[questionId] = true;
            } else {
                delete part1Answers[questionId];
            }
            
            document.getElementById('selectedCount').textContent = Object.keys(part1Answers).length;
            updateNavigation();
        }

        // Handle Part 2 answer
        function handlePart2Answer(answer) {
            part2Answer = answer;
            updateNavigation();
        }

        // Handle Part 3 answer
        function handlePart3Answer(answer) {
            part3Answer = answer;
            updateNavigation();
        }

        // Update progress bar
        function updateProgress() {
            const progress = (currentStep / 3) * 100;
            document.getElementById('progressBar').style.width = `${progress}%`;
            document.getElementById('progressText').textContent = `Etapa ${currentStep} de 3`;
        }

        // Update step indicators
        function updateStepIndicators() {
            for (let i = 1; i <= 3; i++) {
                const indicator = document.getElementById(`step${i}`);
                if (i === currentStep) {
                    indicator.classList.add('active');
                } else {
                    indicator.classList.remove('active');
                }
            }
        }

        // Update navigation buttons
        function updateNavigation() {
            const prevBtn = document.getElementById('prevBtn');
            const nextBtn = document.getElementById('nextBtn');
            
            prevBtn.disabled = currentStep === 1;
            
            if (currentStep === 1) {
                nextBtn.disabled = Object.keys(part1Answers).length === 0;
                nextBtn.textContent = 'Próxima →';
            } else if (currentStep === 2) {
                nextBtn.disabled = part2Answer === null;
                nextBtn.textContent = 'Próxima →';
            } else if (currentStep === 3) {
                nextBtn.disabled = part3Answer === null;
                nextBtn.textContent = '📊 Ver Resultados';
            }
        }

        // Navigation functions
        function prevStep() {
            if (currentStep > 1) {
                currentStep--;
                renderCurrentStep();
                updateProgress();
                updateNavigation();
                updateStepIndicators();
            }
        }

        function nextStep() {
            if (currentStep < 3) {
                currentStep++;
                renderCurrentStep();
                updateProgress();
                updateNavigation();
                updateStepIndicators();
            } else {
                showResults();
            }
        }

        // Calculate and show results
        function showResults() {
            const symptomCount = Object.keys(part1Answers).length;
            const clusteredSymptoms = part2Answer === 'yes';
            const functionalImpairment = part3Answer;
            
            // MDQ scoring criteria
            const positiveScreen = symptomCount >= 7 && clusteredSymptoms && 
                                 (functionalImpairment === 'moderate' || functionalImpairment === 'serious');
            
            let result = '';
            let color = '';
            let recommendation = '';
            let riskLevel = '';

            if (positiveScreen) {
                result = 'Triagem Positiva para Transtorno Bipolar';
                color = 'red';
                riskLevel = 'Alto';
                recommendation = 'Seus resultados sugerem a presença de sintomas consistentes com transtorno bipolar. É altamente recomendável que procure avaliação com um psiquiatra especializado.';
            } else if (symptomCount >= 7 && clusteredSymptoms) {
                result = 'Possível Transtorno Bipolar';
                color = 'orange';
                riskLevel = 'Moderado';
                recommendation = 'Você apresenta sintomas que podem estar relacionados ao transtorno bipolar, mas com menor impacto funcional. Considere uma avaliação profissional.';
            } else if (symptomCount >= 4) {
                result = 'Alguns Sintomas Presentes';
                color = 'yellow';
                riskLevel = 'Baixo a Moderado';
                recommendation = 'Você apresenta alguns sintomas relacionados a mudanças de humor, mas não atendem aos critérios completos. Mantenha-se atento e considere conversar com um profissional se os sintomas piorarem.';
            } else {
                result = 'Triagem Negativa';
                color = 'green';
                riskLevel = 'Baixo';
                recommendation = 'Seus resultados não indicam presença significativa de sintomas de transtorno bipolar. Continue mantendo hábitos saudáveis.';
            }

            document.getElementById('testForm').classList.add('hidden');
            document.getElementById('navigationButtons').classList.add('hidden');
            
            const resultsSection = document.getElementById('resultsSection');
            const resultContent = document.getElementById('resultContent');
            
            resultContent.innerHTML = `
                <div class="text-center mb-8">
                    <div class="inline-block p-6 bg-${color}-100 rounded-full mb-4">
                        <span class="text-4xl">
                            ${color === 'green' ? '😊' : color === 'yellow' ? '😐' : color === 'orange' ? '😕' : '🔄'}
                        </span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">${result}</h3>
                    <div class="text-lg text-${color}-600 font-semibold">Nível de Risco: ${riskLevel}</div>
                </div>
                
                <div class="bg-${color}-50 p-6 rounded-xl mb-8">
                    <h4 class="text-lg font-semibold text-${color}-800 mb-3">💡 Recomendações</h4>
                    <p class="text-${color}-700">${recommendation}</p>
                </div>
                
                <div class="grid md:grid-cols-3 gap-6 mb-8">
                    <div class="bg-purple-50 p-6 rounded-xl text-center">
                        <div class="text-3xl font-bold text-purple-600">${symptomCount}</div>
                        <div class="text-sm text-purple-700">Sintomas Identificados</div>
                        <div class="text-xs text-purple-600">de 13 possíveis</div>
                    </div>
                    
                    <div class="bg-blue-50 p-6 rounded-xl text-center">
                        <div class="text-2xl font-bold text-blue-600">
                            ${clusteredSymptoms ? '✅' : '❌'}
                        </div>
                        <div class="text-sm text-blue-700">Sintomas Agrupados</div>
                        <div class="text-xs text-blue-600">${clusteredSymptoms ? 'Mesmo período' : 'Períodos diferentes'}</div>
                    </div>
                    
                    <div class="bg-indigo-50 p-6 rounded-xl text-center">
                        <div class="text-2xl font-bold text-indigo-600">
                            ${functionalImpairment === 'serious' ? '🔴' : 
                              functionalImpairment === 'moderate' ? '🟠' : 
                              functionalImpairment === 'minor' ? '🟡' : '🟢'}
                        </div>
                        <div class="text-sm text-indigo-700">Prejuízo Funcional</div>
                        <div class="text-xs text-indigo-600">
                            ${functionalImpairment === 'serious' ? 'Sério' : 
                              functionalImpairment === 'moderate' ? 'Moderado' : 
                              functionalImpairment === 'minor' ? 'Menor' : 'Sem problemas'}
                        </div>
                    </div>
                </div>
                
                <div class="bg-gray-50 p-6 rounded-xl mb-6">
                    <h4 class="text-lg font-semibold text-gray-800 mb-4">📊 Critérios MDQ</h4>
                    <div class="space-y-3">
                        <div class="flex items-center justify-between p-3 bg-white rounded-lg">
                            <span class="text-gray-700">≥7 sintomas presentes</span>
                            <span class="${symptomCount >= 7 ? 'text-green-600' : 'text-red-600'} font-bold">
                                ${symptomCount >= 7 ? '✅' : '❌'} (${symptomCount}/7)
                            </span>
                        </div>
                        <div class="flex items-center justify-between p-3 bg-white rounded-lg">
                            <span class="text-gray-700">Sintomas no mesmo período</span>
                            <span class="${clusteredSymptoms ? 'text-green-600' : 'text-red-600'} font-bold">
                                ${clusteredSymptoms ? '✅' : '❌'}
                            </span>
                        </div>
                        <div class="flex items-center justify-between p-3 bg-white rounded-lg">
                            <span class="text-gray-700">Prejuízo moderado/sério</span>
                            <span class="${(functionalImpairment === 'moderate' || functionalImpairment === 'serious') ? 'text-green-600' : 'text-red-600'} font-bold">
                                ${(functionalImpairment === 'moderate' || functionalImpairment === 'serious') ? '✅' : '❌'}
                            </span>
                        </div>
                    </div>
                    <div class="mt-4 p-3 bg-${positiveScreen ? 'red' : 'blue'}-50 rounded-lg">
                        <p class="text-sm text-${positiveScreen ? 'red' : 'blue'}-700">
                            <strong>Resultado Final:</strong> ${positiveScreen ? 'POSITIVO - Todos os critérios atendidos' : 'NEGATIVO - Nem todos os critérios atendidos'}
                        </p>
                    </div>
                </div>
                
                <div class="bg-gray-50 p-6 rounded-xl">
                    <h4 class="text-lg font-semibold text-gray-800 mb-4">📈 Análise Detalhada</h4>
                    <div class="space-y-2 text-sm text-gray-700">
                        <p><strong>Teste aplicado:</strong> MDQ (Mood Disorder Questionnaire)</p>
                        <p><strong>Data da avaliação:</strong> ${new Date().toLocaleDateString('pt-BR')}</p>
                        <p><strong>Sintomas identificados:</strong> ${symptomCount}/13</p>
                        <p><strong>Sintomas agrupados:</strong> ${clusteredSymptoms ? 'Sim' : 'Não'}</p>
                        <p><strong>Prejuízo funcional:</strong> ${functionalImpairment === 'serious' ? 'Sério' : 
                              functionalImpairment === 'moderate' ? 'Moderado' : 
                              functionalImpairment === 'minor' ? 'Menor' : 'Ausente'}</p>
                        <p><strong>Resultado da triagem:</strong> ${positiveScreen ? 'Positivo' : 'Negativo'}</p>
                    </div>
                </div>
                
                ${positiveScreen ? `
                    <div class="mt-6 p-6 bg-red-50 border-2 border-red-200 rounded-xl">
                        <h4 class="text-lg font-bold text-red-800 mb-3">🚨 Ação Recomendada</h4>
                        <p class="text-red-700 mb-3">
                            Seus resultados sugerem fortemente a presença de transtorno bipolar. É muito importante:
                        </p>
                        <ul class="list-disc list-inside text-red-700 space-y-1">
                            <li>Procurar um psiquiatra especialista em transtornos do humor</li>
                            <li>Levar estes resultados para a consulta</li>
                            <li>Não interromper tratamentos atuais sem orientação médica</li>
                            <li>Evitar automedicação</li>
                        </ul>
                    </div>
                ` : ''}
            `;
            
            resultsSection.classList.remove('hidden');
        }

        // Reset test
        function resetTest() {
            currentStep = 1;
            part1Answers = {};
            part2Answer = null;
            part3Answer = null;
            document.getElementById('testForm').classList.remove('hidden');
            document.getElementById('navigationButtons').classList.remove('hidden');
            document.getElementById('resultsSection').classList.add('hidden');
            initTest();
        }

        // Go to assessment center
        function goToCenter() {
            window.location.href = '/centro-avaliacao';
        }

        // Event listeners
        document.getElementById('prevBtn').addEventListener('click', prevStep);
        document.getElementById('nextBtn').addEventListener('click', nextStep);

        // Initialize the test
        initTest();
    </script>
</body>
</html>