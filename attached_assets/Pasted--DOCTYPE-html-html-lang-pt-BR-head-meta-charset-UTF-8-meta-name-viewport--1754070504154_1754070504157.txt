<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Triagem Psiquiátrica Geral</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .question-card {
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .question-card.answered {
            border-color: #10b981;
            background-color: #f0fdf4;
        }
        
        .radio-option {
            transition: all 0.2s ease;
        }
        
        .radio-option:hover {
            background-color: #f1f5f9;
        }
        
        .progress-bar {
            transition: width 0.3s ease;
        }
        
        .result-card {
            animation: slideIn 0.5s ease-out;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .hidden {
            display: none;
        }
        
        .category-icon {
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body>
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-4xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-8">
                <div class="inline-block bg-white/20 text-white text-sm font-bold px-4 py-2 rounded-full mb-4">
                    🧠 TRIAGEM PSIQUIÁTRICA ABRANGENTE
                </div>
                <h1 class="text-4xl font-bold text-white mb-4">
                    <span class="category-icon">🧠</span> Triagem Psiquiátrica Geral
                </h1>
                <p class="text-xl text-white/90 max-w-3xl mx-auto">
                    Avaliação inicial abrangente para identificação de transtornos mentais comuns
                </p>
                <p class="text-white/70 mt-2">
                    Este questionário investiga diferentes áreas da saúde mental para uma triagem completa
                </p>
            </div>

            <!-- Category Overview -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
                <div class="bg-white/10 backdrop-blur-lg rounded-xl p-4 text-center">
                    <div class="text-2xl mb-2">😔</div>
                    <div class="text-white text-sm font-medium">Humor</div>
                </div>
                <div class="bg-white/10 backdrop-blur-lg rounded-xl p-4 text-center">
                    <div class="text-2xl mb-2">😰</div>
                    <div class="text-white text-sm font-medium">Ansiedade</div>
                </div>
                <div class="bg-white/10 backdrop-blur-lg rounded-xl p-4 text-center">
                    <div class="text-2xl mb-2">🧠</div>
                    <div class="text-white text-sm font-medium">Cognição</div>
                </div>
                <div class="bg-white/10 backdrop-blur-lg rounded-xl p-4 text-center">
                    <div class="text-2xl mb-2">💭</div>
                    <div class="text-white text-sm font-medium">Psicose</div>
                </div>
            </div>

            <!-- Progress Bar -->
            <div class="bg-white/20 rounded-full h-3 mb-8">
                <div id="progressBar" class="progress-bar bg-white h-3 rounded-full" style="width: 0%"></div>
            </div>
            <div class="text-center text-white mb-8">
                <span id="progressText">Pergunta 1 de 20</span>
            </div>

            <!-- Test Form -->
            <div id="testForm" class="space-y-6">
                <!-- Questions will be generated by JavaScript -->
            </div>

            <!-- Navigation Buttons -->
            <div id="navigationButtons" class="flex justify-between mt-8">
                <button id="prevBtn" class="bg-white/20 hover:bg-white/30 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    ← Anterior
                </button>
                <button id="nextBtn" class="bg-slate-600 hover:bg-slate-700 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    Próxima →
                </button>
            </div>

            <!-- Results Section (Hidden initially) -->
            <div id="resultsSection" class="hidden">
                <div class="result-card bg-white rounded-2xl p-8 mt-8">
                    <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
                        📊 Resultados - Triagem Psiquiátrica
                    </h2>
                    
                    <div id="resultContent" class="space-y-6">
                        <!-- Results will be populated by JavaScript -->
                    </div>
                    
                    <div class="mt-8 p-6 bg-slate-50 border-l-4 border-slate-400 rounded-r-lg">
                        <h3 class="text-lg font-semibold text-slate-800 mb-2">⚠️ Importante</h3>
                        <p class="text-slate-700">
                            Esta triagem é uma ferramenta de identificação inicial e não substitui a avaliação completa de um profissional de saúde mental. 
                            Os resultados devem ser interpretados por um psiquiatra ou psicólogo qualificado.
                        </p>
                    </div>
                    
                    <div class="text-center mt-8 space-x-4">
                        <button onclick="resetTest()" class="bg-slate-600 hover:bg-slate-700 text-white px-8 py-3 rounded-lg transition-colors">
                            🔄 Fazer Teste Novamente
                        </button>
                        <button onclick="goToCenter()" class="bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-lg transition-colors">
                            🧠 Outros Testes
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Comprehensive Psychiatric Screening Questions
        const questions = [
            // Humor/Depressão (5 perguntas)
            { 
                id: 1, 
                text: "Nas últimas 2 semanas, você se sentiu desanimado, deprimido ou sem esperança?",
                category: "humor",
                icon: "😔"
            },
            { 
                id: 2, 
                text: "Nas últimas 2 semanas, você teve pouco interesse ou prazer em fazer as coisas?",
                category: "humor",
                icon: "😔"
            },
            { 
                id: 3, 
                text: "Você já teve períodos em que se sentiu excessivamente alegre, energético ou eufórico por vários dias?",
                category: "humor",
                icon: "😔"
            },
            { 
                id: 4, 
                text: "Você tem pensado frequentemente sobre morte ou em se machucar?",
                category: "humor",
                icon: "😔"
            },
            { 
                id: 5, 
                text: "Seu humor muda drasticamente sem motivo aparente?",
                category: "humor",
                icon: "😔"
            },

            // Ansiedade (4 perguntas)
            { 
                id: 6, 
                text: "Você se sente nervoso, ansioso ou muito tenso na maior parte dos dias?",
                category: "ansiedade",
                icon: "😰"
            },
            { 
                id: 7, 
                text: "Você tem dificuldade para controlar suas preocupações?",
                category: "ansiedade",
                icon: "😰"
            },
            { 
                id: 8, 
                text: "Você evita certas situações por causa do medo ou ansiedade?",
                category: "ansiedade",
                icon: "😰"
            },
            { 
                id: 9, 
                text: "Você já teve ataques de pânico (episódios súbitos de medo intenso)?",
                category: "ansiedade",
                icon: "😰"
            },

            // Cognição/TDAH (4 perguntas)
            { 
                id: 10, 
                text: "Você tem dificuldade para se concentrar ou manter a atenção?",
                category: "cognicao",
                icon: "🧠"
            },
            { 
                id: 11, 
                text: "Você é facilmente distraído por estímulos externos?",
                category: "cognicao",
                icon: "🧠"
            },
            { 
                id: 12, 
                text: "Você tem dificuldade para organizar tarefas ou atividades?",
                category: "cognicao",
                icon: "🧠"
            },
            { 
                id: 13, 
                text: "Você frequentemente perde objetos importantes?",
                category: "cognicao",
                icon: "🧠"
            },

            // Psicose/Esquizofrenia (3 perguntas)
            { 
                id: 14, 
                text: "Você já ouviu vozes que outras pessoas não ouvem?",
                category: "psicose",
                icon: "💭"
            },
            { 
                id: 15, 
                text: "Você já teve a sensação de que outras pessoas estavam conspirando contra você?",
                category: "psicose",
                icon: "💭"
            },
            { 
                id: 16, 
                text: "Você já sentiu que seus pensamentos não eram seus ou estavam sendo controlados?",
                category: "psicose",
                icon: "💭"
            },

            // Uso de Substâncias (2 perguntas)
            { 
                id: 17, 
                text: "Você bebe álcool ou usa outras substâncias para lidar com problemas emocionais?",
                category: "substancias",
                icon: "🍷"
            },
            { 
                id: 18, 
                text: "Seu uso de álcool ou outras substâncias tem causado problemas em sua vida?",
                category: "substancias",
                icon: "🍷"
            },

            // Funcionamento Geral (2 perguntas)
            { 
                id: 19, 
                text: "Seus sintomas interferem significativamente em seu trabalho, estudos ou relacionamentos?",
                category: "funcionamento",
                icon: "⚖️"
            },
            { 
                id: 20, 
                text: "Você sente que precisa de ajuda profissional para lidar com suas dificuldades emocionais?",
                category: "funcionamento",
                icon: "⚖️"
            }
        ];

        const options = [
            { value: 0, text: "Não, nunca" },
            { value: 1, text: "Raramente" },
            { value: 2, text: "Às vezes" },
            { value: 3, text: "Frequentemente" },
            { value: 4, text: "Quase sempre" }
        ];

        let currentQuestion = 0;
        let answers = {};

        // Initialize test
        function initTest() {
            renderQuestion();
            updateProgress();
            updateNavigation();
        }

        // Render current question
        function renderQuestion() {
            const testForm = document.getElementById('testForm');
            const question = questions[currentQuestion];
            
            const categoryColors = {
                'humor': 'blue',
                'ansiedade': 'yellow',
                'cognicao': 'green',
                'psicose': 'purple',
                'substancias': 'red',
                'funcionamento': 'gray'
            };
            
            const categoryNames = {
                'humor': 'Transtornos do Humor',
                'ansiedade': 'Transtornos de Ansiedade',
                'cognicao': 'Transtornos Cognitivos',
                'psicose': 'Transtornos Psicóticos',
                'substancias': 'Uso de Substâncias',
                'funcionamento': 'Funcionamento Geral'
            };
            
            const color = categoryColors[question.category];
            
            testForm.innerHTML = `
                <div class="question-card bg-white rounded-2xl p-8 ${answers[question.id] !== undefined ? 'answered' : ''}">
                    <div class="mb-6">
                        <span class="inline-block bg-${color}-100 text-${color}-800 text-sm font-medium px-3 py-1 rounded-full mb-4">
                            ${question.icon} ${categoryNames[question.category]}
                        </span>
                        <h3 class="text-xl font-semibold text-gray-800 leading-relaxed">
                            ${currentQuestion + 1}. ${question.text}
                        </h3>
                        <p class="text-gray-600 mt-2 text-sm">
                            Esta pergunta investiga sintomas relacionados a ${categoryNames[question.category].toLowerCase()}
                        </p>
                    </div>
                    
                    <div class="space-y-3">
                        ${options.map(option => `
                            <label class="radio-option flex items-center p-4 rounded-lg cursor-pointer border border-gray-200 hover:bg-slate-50">
                                <input type="radio" name="question_${question.id}" value="${option.value}" 
                                       class="w-4 h-4 text-slate-600 border-gray-300 focus:ring-slate-500"
                                       ${answers[question.id] === option.value ? 'checked' : ''}
                                       onchange="handleAnswer(${question.id}, ${option.value})">
                                <span class="ml-3 text-gray-700 font-medium">${option.text}</span>
                            </label>
                        `).join('')}
                    </div>
                    
                    ${question.category === 'psicose' || question.id === 4 ? `
                        <div class="mt-4 p-3 bg-red-50 border border-red-200 rounded-lg">
                            <p class="text-red-700 text-sm font-medium">
                                ⚠️ Esta pergunta investiga sintomas que requerem atenção profissional especializada
                            </p>
                        </div>
                    ` : ''}
                </div>
            `;
        }

        // Handle answer selection
        function handleAnswer(questionId, value) {
            answers[questionId] = value;
            updateProgress();
            updateNavigation();
            
            // Add visual feedback
            const questionCard = document.querySelector('.question-card');
            questionCard.classList.add('answered');
            
            // Special handling for high-risk answers
            if ((questionId === 4 && value > 1) || // Suicidal ideation
                (questionId === 14 && value > 1) || // Auditory hallucinations
                (questionId === 15 && value > 1) || // Paranoid thoughts
                (questionId === 16 && value > 1)) { // Thought control
                setTimeout(() => {
                    alert('⚠️ ATENÇÃO: Sua resposta indica sintomas que requerem avaliação profissional urgente. Considere procurar ajuda psiquiátrica o quanto antes.');
                }, 500);
            }
        }

        // Update progress bar
        function updateProgress() {
            const progress = (Object.keys(answers).length / questions.length) * 100;
            document.getElementById('progressBar').style.width = `${progress}%`;
            document.getElementById('progressText').textContent = `Pergunta ${currentQuestion + 1} de ${questions.length}`;
        }

        // Update navigation buttons
        function updateNavigation() {
            const prevBtn = document.getElementById('prevBtn');
            const nextBtn = document.getElementById('nextBtn');
            
            prevBtn.disabled = currentQuestion === 0;
            
            if (currentQuestion === questions.length - 1) {
                nextBtn.textContent = Object.keys(answers).length === questions.length ? '📊 Ver Resultados' : 'Finalizar';
                nextBtn.disabled = answers[questions[currentQuestion].id] === undefined;
            } else {
                nextBtn.textContent = 'Próxima →';
                nextBtn.disabled = answers[questions[currentQuestion].id] === undefined;
            }
        }

        // Navigation functions
        function prevQuestion() {
            if (currentQuestion > 0) {
                currentQuestion--;
                renderQuestion();
                updateProgress();
                updateNavigation();
            }
        }

        function nextQuestion() {
            if (currentQuestion < questions.length - 1) {
                currentQuestion++;
                renderQuestion();
                updateProgress();
                updateNavigation();
            } else {
                showResults();
            }
        }

        // Calculate and show results
        function showResults() {
            // Calculate scores by category
            const categoryScores = {
                humor: 0,
                ansiedade: 0,
                cognicao: 0,
                psicose: 0,
                substancias: 0,
                funcionamento: 0
            };

            questions.forEach(question => {
                categoryScores[question.category] += answers[question.id];
            });

            // Calculate total score and identify risk areas
            const totalScore = Object.values(answers).reduce((sum, value) => sum + value, 0);
            const maxScore = questions.length * 4;
            
            // Identify high-risk categories (score >= 50% of max for that category)
            const riskAreas = [];
            if (categoryScores.humor >= 10) riskAreas.push('Transtornos do Humor');
            if (categoryScores.ansiedade >= 8) riskAreas.push('Transtornos de Ansiedade');
            if (categoryScores.cognicao >= 8) riskAreas.push('Transtornos Cognitivos/TDAH');
            if (categoryScores.psicose >= 6) riskAreas.push('Sintomas Psicóticos');
            if (categoryScores.substancias >= 4) riskAreas.push('Uso Problemático de Substâncias');
            if (categoryScores.funcionamento >= 4) riskAreas.push('Prejuízo Funcional');

            // Determine overall risk level
            let overallRisk = '';
            let color = '';
            let recommendation = '';

            if (totalScore <= 20) {
                overallRisk = 'Baixo Risco';
                color = 'green';
                recommendation = 'Seus resultados sugerem baixo risco para transtornos mentais. Continue mantendo hábitos saudáveis.';
            } else if (totalScore <= 40) {
                overallRisk = 'Risco Moderado';
                color = 'yellow';
                recommendation = 'Seus resultados indicam risco moderado. Considere uma avaliação preventiva com profissional de saúde mental.';
            } else if (totalScore <= 60) {
                overallRisk = 'Risco Alto';
                color = 'orange';
                recommendation = 'Seus resultados sugerem risco alto para transtornos mentais. É recomendável buscar avaliação profissional.';
            } else {
                overallRisk = 'Risco Muito Alto';
                color = 'red';
                recommendation = 'Seus resultados indicam risco muito alto. É importante procurar ajuda psiquiátrica o quanto antes.';
            }

            // Check for emergency symptoms
            const hasEmergencySymptoms = (answers[4] > 1) || (answers[14] > 1) || (answers[15] > 1) || (answers[16] > 1);

            document.getElementById('testForm').classList.add('hidden');
            document.getElementById('navigationButtons').classList.add('hidden');
            
            const resultsSection = document.getElementById('resultsSection');
            const resultContent = document.getElementById('resultContent');
            
            resultContent.innerHTML = `
                <div class="text-center mb-8">
                    <div class="inline-block p-6 bg-${color}-100 rounded-full mb-4">
                        <span class="text-4xl">
                            ${color === 'green' ? '😊' : color === 'yellow' ? '😐' : color === 'orange' ? '😟' : '😰'}
                        </span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">${overallRisk}</h3>
                    <div class="text-3xl font-bold text-${color}-600">${totalScore}/${maxScore} pontos</div>
                    ${hasEmergencySymptoms ? `
                        <div class="mt-4 p-4 bg-red-100 border-2 border-red-500 rounded-lg">
                            <p class="text-red-800 font-bold">🚨 SINTOMAS DE ALERTA DETECTADOS</p>
                            <p class="text-red-700 text-sm mt-1">Procure ajuda profissional imediatamente</p>
                        </div>
                    ` : ''}
                </div>
                
                <div class="bg-${color}-50 p-6 rounded-xl mb-8">
                    <h4 class="text-lg font-semibold text-${color}-800 mb-3">💡 Recomendações</h4>
                    <p class="text-${color}-700">${recommendation}</p>
                </div>
                
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
                    <div class="bg-blue-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">😔</span>
                            <h5 class="font-semibold text-blue-800">Humor</h5>
                        </div>
                        <div class="text-2xl font-bold text-blue-600">${categoryScores.humor}/20</div>
                        <div class="text-xs text-blue-600">${categoryScores.humor >= 10 ? 'Atenção necessária' : 'Normal'}</div>
                    </div>
                    
                    <div class="bg-yellow-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">😰</span>
                            <h5 class="font-semibold text-yellow-800">Ansiedade</h5>
                        </div>
                        <div class="text-2xl font-bold text-yellow-600">${categoryScores.ansiedade}/16</div>
                        <div class="text-xs text-yellow-600">${categoryScores.ansiedade >= 8 ? 'Atenção necessária' : 'Normal'}</div>
                    </div>
                    
                    <div class="bg-green-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">🧠</span>
                            <h5 class="font-semibold text-green-800">Cognição</h5>
                        </div>
                        <div class="text-2xl font-bold text-green-600">${categoryScores.cognicao}/16</div>
                        <div class="text-xs text-green-600">${categoryScores.cognicao >= 8 ? 'Atenção necessária' : 'Normal'}</div>
                    </div>
                    
                    <div class="bg-purple-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">💭</span>
                            <h5 class="font-semibold text-purple-800">Psicose</h5>
                        </div>
                        <div class="text-2xl font-bold text-purple-600">${categoryScores.psicose}/12</div>
                        <div class="text-xs text-purple-600">${categoryScores.psicose >= 6 ? 'Avaliação urgente' : 'Normal'}</div>
                    </div>
                    
                    <div class="bg-red-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">🍷</span>
                            <h5 class="font-semibold text-red-800">Substâncias</h5>
                        </div>
                        <div class="text-2xl font-bold text-red-600">${categoryScores.substancias}/8</div>
                        <div class="text-xs text-red-600">${categoryScores.substancias >= 4 ? 'Atenção necessária' : 'Normal'}</div>
                    </div>
                    
                    <div class="bg-gray-50 p-4 rounded-xl">
                        <div class="flex items-center mb-2">
                            <span class="text-2xl mr-2">⚖️</span>
                            <h5 class="font-semibold text-gray-800">Funcionamento</h5>
                        </div>
                        <div class="text-2xl font-bold text-gray-600">${categoryScores.funcionamento}/8</div>
                        <div class="text-xs text-gray-600">${categoryScores.funcionamento >= 4 ? 'Prejuízo presente' : 'Adequado'}</div>
                    </div>
                </div>
                
                ${riskAreas.length > 0 ? `
                    <div class="bg-orange-50 p-6 rounded-xl mb-6">
                        <h4 class="text-lg font-semibold text-orange-800 mb-3">⚠️ Áreas de Atenção</h4>
                        <p class="text-orange-700 mb-3">Os seguintes domínios apresentaram pontuações elevadas:</p>
                        <ul class="list-disc list-inside text-orange-700 space-y-1">
                            ${riskAreas.map(area => `<li>${area}</li>`).join('')}
                        </ul>
                    </div>
                ` : ''}
                
                <div class="bg-gray-50 p-6 rounded-xl">
                    <h4 class="text-lg font-semibold text-gray-800 mb-4">📈 Análise Detalhada</h4>
                    <div class="space-y-2 text-sm text-gray-700">
                        <p><strong>Teste aplicado:</strong> Triagem Psiquiátrica Geral Abrangente</p>
                        <p><strong>Data da avaliação:</strong> ${new Date().toLocaleDateString('pt-BR')}</p>
                        <p><strong>Pontuação total:</strong> ${totalScore}/${maxScore} pontos</p>
                        <p><strong>Nível de risco:</strong> ${overallRisk}</p>
                        <p><strong>Áreas de atenção:</strong> ${riskAreas.length > 0 ? riskAreas.join(', ') : 'Nenhuma'}</p>
                        <p><strong>Sintomas de alerta:</strong> ${hasEmergencySymptoms ? 'Presentes ⚠️' : 'Ausentes ✅'}</p>
                    </div>
                </div>
                
                ${hasEmergencySymptoms || color === 'red' ? `
                    <div class="mt-6 p-6 bg-red-50 border-2 border-red-200 rounded-xl">
                        <h4 class="text-lg font-bold text-red-800 mb-3">🚨 Ação Urgente Recomendada</h4>
                        <p class="text-red-700 mb-3">
                            Seus resultados indicam a necessidade de avaliação profissional urgente:
                        </p>
                        <ul class="list-disc list-inside text-red-700 space-y-1">
                            <li>Procurar um psiquiatra o quanto antes</li>
                            <li>Levar estes resultados para a consulta</li>
                            <li>Se há risco imediato, procurar emergência psiquiátrica</li>
                            <li>Informar familiares/pessoas próximas sobre os sintomas</li>
                            ${hasEmergencySymptoms ? '<li>Em caso de ideação suicida: CVV 188 (24h)</li>' : ''}
                        </ul>
                    </div>
                ` : ''}
            `;
            
            resultsSection.classList.remove('hidden');
        }

        // Reset test
        function resetTest() {
            currentQuestion = 0;
            answers = {};
            document.getElementById('testForm').classList.remove('hidden');
            document.getElementById('navigationButtons').classList.remove('hidden');
            document.getElementById('resultsSection').classList.add('hidden');
            initTest();
        }

        // Go to assessment center
        function goToCenter() {
            window.location.href = '/centro-avaliacao';
        }

        // Event listeners
        document.getElementById('prevBtn').addEventListener('click', prevQuestion);
        document.getElementById('nextBtn').addEventListener('click', nextQuestion);

        // Initialize the test
        initTest();
    </script>
</body>
</html>