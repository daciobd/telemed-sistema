<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teste TDAH - ASRS-18</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .question-card {
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .question-card.answered {
            border-color: #10b981;
            background-color: #f0fdf4;
        }
        
        .radio-option {
            transition: all 0.2s ease;
        }
        
        .radio-option:hover {
            background-color: #f3f4f6;
        }
        
        .progress-bar {
            transition: width 0.3s ease;
        }
        
        .result-card {
            animation: slideIn 0.5s ease-out;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .hidden {
            display: none;
        }
    </style>
</head>
<body>
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-4xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-8">
                <h1 class="text-4xl font-bold text-white mb-4">
                    🧠 Teste TDAH - ASRS-18
                </h1>
                <p class="text-xl text-white/90 max-w-3xl mx-auto">
                    Avaliação para Transtorno do Déficit de Atenção e Hiperatividade
                </p>
                <p class="text-white/70 mt-2">
                    Marque a alternativa que melhor descreve a frequência dos seus sintomas nos últimos 6 meses
                </p>
            </div>

            <!-- Progress Bar -->
            <div class="bg-white/20 rounded-full h-3 mb-8">
                <div id="progressBar" class="progress-bar bg-white h-3 rounded-full" style="width: 0%"></div>
            </div>
            <div class="text-center text-white mb-8">
                <span id="progressText">Pergunta 1 de 18</span>
            </div>

            <!-- Test Form -->
            <div id="testForm" class="space-y-6">
                <!-- Questions will be generated by JavaScript -->
            </div>

            <!-- Navigation Buttons -->
            <div id="navigationButtons" class="flex justify-between mt-8">
                <button id="prevBtn" class="bg-white/20 hover:bg-white/30 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    ← Anterior
                </button>
                <button id="nextBtn" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg transition-colors disabled:opacity-50" disabled>
                    Próxima →
                </button>
            </div>

            <!-- Results Section (Hidden initially) -->
            <div id="resultsSection" class="hidden">
                <div class="result-card bg-white rounded-2xl p-8 mt-8">
                    <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
                        📊 Resultados do Teste TDAH
                    </h2>
                    
                    <div id="resultContent" class="space-y-6">
                        <!-- Results will be populated by JavaScript -->
                    </div>
                    
                    <div class="mt-8 p-6 bg-yellow-50 border-l-4 border-yellow-400 rounded-r-lg">
                        <h3 class="text-lg font-semibold text-yellow-800 mb-2">⚠️ Importante</h3>
                        <p class="text-yellow-700">
                            Este teste é apenas uma ferramenta de triagem e não substitui a avaliação de um profissional de saúde mental. 
                            Se você apresenta sintomas significativos, procure um psiquiatra ou psicólogo especializado em TDAH.
                        </p>
                    </div>
                    
                    <div class="text-center mt-8">
                        <button onclick="resetTest()" class="bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-lg transition-colors">
                            🔄 Fazer Teste Novamente
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Questions data
        const questions = [
            // Parte A - Desatenção
            { id: 1, text: "Com que frequência você deixa de prestar atenção a detalhes ou comete erros por descuido no trabalho ou em outras atividades?", category: "desatencao" },
            { id: 2, text: "Com que frequência você tem dificuldade para manter a atenção em tarefas longas (ex.: leitura, reuniões)?", category: "desatencao" },
            { id: 3, text: "Com que frequência você parece não ouvir quando falam diretamente com você?", category: "desatencao" },
            { id: 4, text: "Com que frequência você não segue instruções até o fim e não termina tarefas (ex.: perde o foco no meio)?", category: "desatencao" },
            { id: 5, text: "Com que frequência você tem dificuldade para organizar tarefas ou atividades?", category: "desatencao" },
            { id: 6, text: "Com que frequência você evita ou adia tarefas que exigem esforço mental prolongado (ex.: relatórios)?", category: "desatencao" },
            { id: 7, text: "Com que frequência você perde objetos necessários para tarefas (ex.: chaves, celular)?", category: "desatencao" },
            { id: 8, text: "Com que frequência você se distrai facilmente com estímulos externos?", category: "desatencao" },
            { id: 9, text: "Com que frequência você esquece compromissos ou atividades cotidianas?", category: "desatencao" },
            
            // Parte B - Hiperatividade/Impulsividade
            { id: 10, text: "Com que frequência você agita ou bate as mãos/pés quando sentado por muito tempo?", category: "hiperatividade" },
            { id: 11, text: "Com que frequência você levanta da cadeira em situações onde deveria permanecer sentado?", category: "hiperatividade" },
            { id: 12, text: "Com que frequência você se sente inquieto ou \"a todo vapor\"?", category: "hiperatividade" },
            { id: 13, text: "Com que frequência você tem dificuldade para envolver-se em atividades de lazer calmamente?", category: "hiperatividade" },
            { id: 14, text: "Com que frequência você age como se estivesse \"movido a motor\"?", category: "hiperatividade" },
            { id: 15, text: "Com que frequência você fala excessivamente?", category: "hiperatividade" },
            { id: 16, text: "Com que frequência você responde antes da pergunta ser concluída?", category: "hiperatividade" },
            { id: 17, text: "Com que frequência você tem dificuldade para esperar a sua vez?", category: "hiperatividade" },
            { id: 18, text: "Com que frequência você interrompe ou se intromete em conversas alheias?", category: "hiperatividade" }
        ];

        const options = [
            { value: 0, text: "Nunca" },
            { value: 1, text: "Raramente" },
            { value: 2, text: "Às vezes" },
            { value: 3, text: "Frequentemente" },
            { value: 4, text: "Muito frequentemente" }
        ];

        let currentQuestion = 0;
        let answers = {};

        // Initialize test
        function initTest() {
            renderQuestion();
            updateProgress();
            updateNavigation();
        }

        // Render current question
        function renderQuestion() {
            const testForm = document.getElementById('testForm');
            const question = questions[currentQuestion];
            
            testForm.innerHTML = `
                <div class="question-card bg-white rounded-2xl p-8 ${answers[question.id] !== undefined ? 'answered' : ''}">
                    <div class="mb-6">
                        <span class="inline-block bg-purple-100 text-purple-800 text-sm font-medium px-3 py-1 rounded-full mb-4">
                            ${question.category === 'desatencao' ? '🎯 Desatenção' : '⚡ Hiperatividade/Impulsividade'}
                        </span>
                        <h3 class="text-xl font-semibold text-gray-800 leading-relaxed">
                            ${currentQuestion + 1}. ${question.text}
                        </h3>
                    </div>
                    
                    <div class="space-y-3">
                        ${options.map(option => `
                            <label class="radio-option flex items-center p-3 rounded-lg cursor-pointer border border-gray-200 hover:bg-gray-50">
                                <input type="radio" name="question_${question.id}" value="${option.value}" 
                                       class="w-4 h-4 text-purple-600 border-gray-300 focus:ring-purple-500"
                                       ${answers[question.id] === option.value ? 'checked' : ''}
                                       onchange="handleAnswer(${question.id}, ${option.value})">
                                <span class="ml-3 text-gray-700 font-medium">${option.text}</span>
                            </label>
                        `).join('')}
                    </div>
                </div>
            `;
        }

        // Handle answer selection
        function handleAnswer(questionId, value) {
            answers[questionId] = value;
            updateProgress();
            updateNavigation();
            
            // Add visual feedback
            const questionCard = document.querySelector('.question-card');
            questionCard.classList.add('answered');
        }

        // Update progress bar
        function updateProgress() {
            const progress = (Object.keys(answers).length / questions.length) * 100;
            document.getElementById('progressBar').style.width = `${progress}%`;
            document.getElementById('progressText').textContent = `Pergunta ${currentQuestion + 1} de ${questions.length}`;
        }

        // Update navigation buttons
        function updateNavigation() {
            const prevBtn = document.getElementById('prevBtn');
            const nextBtn = document.getElementById('nextBtn');
            
            prevBtn.disabled = currentQuestion === 0;
            
            if (currentQuestion === questions.length - 1) {
                nextBtn.textContent = Object.keys(answers).length === questions.length ? '📊 Ver Resultados' : 'Finalizar';
                nextBtn.disabled = answers[questions[currentQuestion].id] === undefined;
            } else {
                nextBtn.textContent = 'Próxima →';
                nextBtn.disabled = answers[questions[currentQuestion].id] === undefined;
            }
        }

        // Navigation functions
        function prevQuestion() {
            if (currentQuestion > 0) {
                currentQuestion--;
                renderQuestion();
                updateProgress();
                updateNavigation();
            }
        }

        function nextQuestion() {
            if (currentQuestion < questions.length - 1) {
                currentQuestion++;
                renderQuestion();
                updateProgress();
                updateNavigation();
            } else {
                showResults();
            }
        }

        // Calculate and show results
        function showResults() {
            const desatencaoScore = questions
                .filter(q => q.category === 'desatencao')
                .reduce((sum, q) => sum + (answers[q.id] >= 3 ? 1 : 0), 0);
            
            const hiperatividadeScore = questions
                .filter(q => q.category === 'hiperatividade')
                .reduce((sum, q) => sum + (answers[q.id] >= 3 ? 1 : 0), 0);

            let diagnosis = '';
            let color = '';
            let recommendation = '';

            if (desatencaoScore >= 6 && hiperatividadeScore >= 6) {
                diagnosis = 'TDAH Tipo Combinado';
                color = 'red';
                recommendation = 'Você apresenta sintomas significativos tanto de desatenção quanto de hiperatividade/impulsividade. É altamente recomendável buscar avaliação profissional.';
            } else if (desatencaoScore >= 6) {
                diagnosis = 'TDAH Tipo Desatento';
                color = 'orange';
                recommendation = 'Você apresenta sintomas significativos de desatenção. Recomenda-se avaliação com profissional especializado.';
            } else if (hiperatividadeScore >= 6) {
                diagnosis = 'TDAH Tipo Hiperativo/Impulsivo';
                color = 'yellow';
                recommendation = 'Você apresenta sintomas significativos de hiperatividade/impulsividade. Procure orientação profissional.';
            } else {
                diagnosis = 'Sintomas Abaixo do Critério';
                color = 'green';
                recommendation = 'Seus sintomas não atendem aos critérios para TDAH. Se ainda tem preocupações, consulte um profissional.';
            }

            document.getElementById('testForm').classList.add('hidden');
            document.getElementById('navigationButtons').classList.add('hidden');
            
            const resultsSection = document.getElementById('resultsSection');
            const resultContent = document.getElementById('resultContent');
            
            resultContent.innerHTML = `
                <div class="text-center mb-8">
                    <div class="inline-block p-6 bg-${color}-100 rounded-full mb-4">
                        <span class="text-4xl">
                            ${color === 'green' ? '✅' : color === 'yellow' ? '⚠️' : color === 'orange' ? '🔶' : '🔴'}
                        </span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">${diagnosis}</h3>
                    <p class="text-gray-600">${recommendation}</p>
                </div>
                
                <div class="grid md:grid-cols-2 gap-6 mb-8">
                    <div class="bg-blue-50 p-6 rounded-xl">
                        <h4 class="text-lg font-semibold text-blue-800 mb-2">🎯 Desatenção</h4>
                        <div class="text-3xl font-bold text-blue-600">${desatencaoScore}/9</div>
                        <p class="text-sm text-blue-700">sintomas significativos</p>
                        <div class="mt-2 bg-blue-200 rounded-full h-2">
                            <div class="bg-blue-600 h-2 rounded-full" style="width: ${(desatencaoScore/9)*100}%"></div>
                        </div>
                    </div>
                    
                    <div class="bg-purple-50 p-6 rounded-xl">
                        <h4 class="text-lg font-semibold text-purple-800 mb-2">⚡ Hiperatividade/Impulsividade</h4>
                        <div class="text-3xl font-bold text-purple-600">${hiperatividadeScore}/9</div>
                        <p class="text-sm text-purple-700">sintomas significativos</p>
                        <div class="mt-2 bg-purple-200 rounded-full h-2">
                            <div class="bg-purple-600 h-2 rounded-full" style="width: ${(hiperatividadeScore/9)*100}%"></div>
                        </div>
                    </div>
                </div>
                
                <div class="bg-gray-50 p-6 rounded-xl">
                    <h4 class="text-lg font-semibold text-gray-800 mb-4">📈 Análise Detalhada</h4>
                    <div class="space-y-2 text-sm text-gray-700">
                        <p><strong>Total de respostas:</strong> ${Object.keys(answers).length}/18</p>
                        <p><strong>Critério DSM-5:</strong> ≥6 sintomas "Frequentemente/Muito frequentemente" em cada categoria</p>
                        <p><strong>Data da avaliação:</strong> ${new Date().toLocaleDateString('pt-BR')}</p>
                    </div>
                </div>
            `;
            
            resultsSection.classList.remove('hidden');
        }

        // Reset test
        function resetTest() {
            currentQuestion = 0;
            answers = {};
            document.getElementById('testForm').classList.remove('hidden');
            document.getElementById('navigationButtons').classList.remove('hidden');
            document.getElementById('resultsSection').classList.add('hidden');
            initTest();
        }

        // Event listeners
        document.getElementById('prevBtn').addEventListener('click', prevQuestion);
        document.getElementById('nextBtn').addEventListener('click', nextQuestion);

        // Initialize the test
        initTest();
    </script>
</body>
</html>