<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TeleMed - Consultas com Especialistas</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 20px;
      background: linear-gradient(#e0eafc, #cfdef3);
      color: #2c3e50;
      min-height: 100vh;
    }
    .container {
      max-width: 900px;
      margin: 0 auto;
      background-color: rgba(255, 255, 255, 0.9);
      border-radius: 15px;
      padding: 20px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
      text-align: center;
      color: #2980b9;
    }
    .section {
      margin-bottom: 30px;
      padding: 15px;
      background-color: #fff;
      border-radius: 10px;
    }
    .section h2 {
      color: #3498db;
      margin-top: 0;
    }
    .section p {
      line-height: 1.6;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .form-group label {
      display: block;
      margin-bottom: 5px;
    }
    .form-group select, .form-group input {
      width: 100%;
      padding: 8px;
      border: 1px solid #ddd;
      border-radius: 5px;
      font-size: 14px;
    }
    .form-group button {
      background-color: #3498db;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    .form-group button:hover {
      background-color: #2980b9;
    }
    #lances {
      margin-top: 10px;
      list-style: none;
      padding: 0;
    }
    #lances li {
      padding: 5px 0;
      border-bottom: 1px solid #eee;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>TeleMed - Consultas com Especialistas</h1>

    <div class="section">
      <h2>Defina o Valor da Sua Consulta com Especialistas Certificados</h2>
      <p>Ofereça a partir de R$ 150,00 e médicos experientes com RQE do CRM competirão por sua consulta. Valores maiores garantem atendimento mais rápido! Diferente do PicDoc, você pode agendar com o mesmo médico futuramente se gostar do atendimento.</p>
    </div>

    <div class="section">
      <h2>Como Funciona</h2>
      <p><strong>🎯 Como Funciona o Lance:</strong> Escolha sua especialidade, defina um valor inicial e envie sua proposta. Médicos veem sua oferta e o primeiro a aceitar fica com a consulta. Valores maiores (ex.: R$ 200+) são aceitos em 5-15 minutos. Para valores menores, médicos podem marcar consultas futuras.</p>
      <p><strong>⚖️ Equilíbrio Preço × Velocidade:</strong> R$ 150,00 pode demorar 1-2 horas ou ser agendado para outro horário; R$ 200,00 em 30-60 minutos; R$ 280,00 em 5-15 minutos. Para urgência, adicione 30-40%.</p>
    </div>

    <div class="section">
      <h2>Defina Sua Consulta</h2>
      <form id="leilaoForm" onsubmit="fazerLance(event)">
        <div class="form-group">
          <label for="especialidade">Especialidade:</label>
          <select id="especialidade" required>
            <option value="clinica">Clínica Geral (R$ 150-200)</option>
            <option value="psiquiatria">Psiquiatria (R$ 150-280)</option>
            <option value="cardiologia">Cardiologia (R$ 180-250)</option>
            <option value="pediatria">Pediatria (R$ 80-180)</option>
            <option value="dermatologia">Dermatologia (R$ 120-200)</option>
            <option value="ginecologia">Ginecologia (R$ 120-240)</option>
          </select>
        </div>
        <div class="form-group">
          <label for="valor">Valor (mínimo R$ 150,00):</label>
          <input type="number" id="valor" min="150" step="10" required>
        </div>
        <div class="form-group">
          <label>
            <input type="checkbox" id="urgente"> Marcar como Urgente (+30-40%)
          </input>
          </label>
        </div>
        <div class="form-group">
          <button type="submit">Enviar Proposta</button>
        </div>
      </form>
      <ul id="lances"></ul>
    </div>
  </div>

  <script>
    let lances = [];

    function fazerLance(event) {
      event.preventDefault();
      const especialidade = document.getElementById('especialidade').value;
      let valor = parseInt(document.getElementById('valor').value);
      const urgente = document.getElementById('urgente').checked;

      if (valor < 150) {
        alert("O valor mínimo é R$ 150,00!");
        return;
      }

      if (urgente) {
        valor = Math.round(valor * 1.35); // Aumenta 35% para urgência
        alert(`Urgência ativada! Novo valor: R$ ${valor},00. Aceitação em 5-15 minutos.`);
      } else if (valor === 150) {
        alert(`Valor R$ ${valor},00 enviado! Pode ser agendado para outro horário ou demorar 1-2 horas.`);
      } else {
        alert(`Proposta enviada para ${especialidade}! Aceitação estimada: ${valor >= 280 ? '5-15 min' : valor >= 200 ? '30-60 min' : '1-2 horas'}.`);
      }

      const lance = {
        especialidade,
        valor,
        hora: new Date().toLocaleTimeString(),
      };
      lances.push(lance);
      atualizarLances();

      // Simulação de aceitação (remover em produção)
      setTimeout(() => {
        if (Math.random() > 0.3) {
          alert(`Consulta para ${especialidade} aceita por R$ ${valor},00 às ${new Date().toLocaleTimeString()}! Você pode agendar com este médico novamente.`);
        } else if (valor === 150) {
          alert(`Nenhum médico disponível agora. Consulta agendada para amanhã às 10:00.`);
        } else {
          alert(`Nenhum médico aceitou ainda. Ajuste o valor ou aguarde.`);
        }
      }, urgente ? 5000 : 60000);
    }

    function atualizarLances() {
      const lista = document.getElementById('lances');
      lista.innerHTML = lances.map(l => `<li>${l.especialidade}: R$ ${l.valor},00 às ${l.hora}</li>`).join('');
    }
  </script>
</body>
</html>