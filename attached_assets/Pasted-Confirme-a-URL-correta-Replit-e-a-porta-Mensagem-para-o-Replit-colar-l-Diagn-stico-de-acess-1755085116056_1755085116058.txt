Confirme a URL correta (Replit) e a porta
Mensagem para o Replit (colar lá):

✅ Diagnóstico de acesso externo

Garanta que o servidor escuta em process.env.PORT || 5000 e host 0.0.0.0.

Imprima no console a URL pública do Replit (ex.: https://<app>.<user>.repl.co).

Liste as rotas ativas /, /video-consultation, /api/status, /perf/*.

Responda com a URL pública para eu abrir fora do Replit.

O que você faz fora do Replit:

Abra a URL pública do Replit que ele te retornar (não localhost).

Ex.: https://seu-projeto.seu-user.repl.co/video-consultation?consultationId=demo

Abra também …/perf/video-baseline.html.

Por quê: localhost sempre aponta pro seu computador. O Replit roda em outro lugar; por isso ele te dá um domínio público.

2) Se você está rodando localmente (não na nuvem)
Peça ao Replit para garantir o bind correto:

Mensagem para o Replit (usar só se o projeto é Node/Express, que é o nosso caso):

Ajuste de bind/porta no Express:

ts
Copiar
Editar
// server/index.ts
const PORT = process.env.PORT ? Number(process.env.PORT) : 5000;
const HOST = '0.0.0.0';
app.listen(PORT, HOST, () => {
  console.log(`[server] listening on http://${HOST}:${PORT}`);
});
// servir relatórios
import path from 'path';
import express from 'express';
app.use('/perf', express.static(path.join(process.cwd(), 'perf')));
// fallback de SPA (se a rota é client-side)
app.use(express.static(path.join(process.cwd(), 'dist')));
app.get(['/video-consultation', '/enhanced-consultation'], (_req,res)=>{
  res.sendFile(path.join(process.cwd(), 'dist', 'index.html'));
});
Reinicie o servidor e confirme no log a porta/host.

(Se fosse Flask, alternativa rápida):

py
Copiar
Editar
app.run(host="0.0.0.0", port=int(os.getenv("PORT", 5000)), debug=True)
Você fora do Replit (rodando local):

Acesse http://localhost:5000/api/status primeiro.

Depois http://localhost:5000/video-consultation?consultationId=demo.

Se ainda falhar: tente a porta que aparecer no log (às vezes 3000/8080).

3) Garanta que a rota realmente existe (SPA vs. server)
Se a página é SPA (React) e você acessa /video-consultation direto, o servidor precisa entregar index.html (fallback). O snippet acima já faz isso. Sem esse fallback, você verá erro mesmo com o servidor ativo.